[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: TanStack Router owns client-side routes (e.g. /resume/:id/preview).
# Without this, a direct load or refresh on those paths 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
